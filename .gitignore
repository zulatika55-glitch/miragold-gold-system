# dependencies
/node_modules

# next.js build output
/.next/

# typescript incremental build cache (churns every build, not meant to be tracked)
*.tsbuildinfo

# local env / secrets — never commit
.env
.env.local
.env.*.local
